# Open in Xcode

Use the project at:

`clients/ios-xcode-shell-client/DeviceInspectorShell.xcodeproj`

The shell includes a minimal project file, workspace metadata, SwiftUI source files, synthetic case models, local sample data, and a placeholder API client.

Local Xcode validation is still required before presenting it as a compiled app.
